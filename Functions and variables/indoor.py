# **Task description**
# Prompts the user for a string input and outputs it entirely 
# in lowercase, leaving punctuation and whitespace unchanged.

# Prompt user for input
voice = input('Pls enter text: ')

# Output the input in lowercase (punctuation and whitespace remains unchanged)
print(voice.lower())
